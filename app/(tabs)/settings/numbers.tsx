import { View, Text } from 'react-native'
import React from 'react'
import NotVerified from '@/components/not-verifed'

export default function numbers() {
  return (
    <View className='flex-1'>
      <NotVerified />
    </View>
  )
}